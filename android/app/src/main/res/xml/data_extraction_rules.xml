<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ (API 31) backup rules.
  Everything is excluded on purpose: the stored device identifier must NOT be
  transferable to another handset via cloud backup or device-to-device transfer,
  otherwise the "one employee = one phone" guarantee can be bypassed.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
